#!/usr/bin/env bash
set -euo pipefail
command -v nagsly-monitor-gmail >/dev/null 2>&1 || {
  echo 'nagsly-sync-gmail: nagsly-monitor-gmail is not installed on PATH' >&2
  exit 127
}
exec nagsly-monitor-gmail --sync
